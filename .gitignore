node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Raw broker exports — kept local, never committed.
# The generated src/data/portfolioData.ts IS committed so the app builds from a clone.
inputs/
